# Changelog

## 1.0.0

- Initial Grok port of the `create-plugin` meta plugin (upstream `11ecc12`).
- Scaffolding and submission-review skills for Grok plugin authoring.
- Plugin architecture agent, quality-gate rule, and `create-plugin` command.
- Adapted paths and manifests from `.cursor-plugin` / `~/.cursor/plugins/local` to `.grok-plugin` / `~/.grok/plugins`.
